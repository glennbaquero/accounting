@extends('master')

@section('content')

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Update Product Specification <small>({{ $item->specification_name }})</small></h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="{{ route('specifications.index') }}">Product Specification</a></li>
                    <li class="breadcrumb-item active"><a href="javascript:void(0)">Update</a></li>
                </ol>
            </div>
        </div>
    </section>

    <!-- Main content -->
    <section class="content">
        <specifications-view
            submit-url="{{ route('specifications.update', $item->id) }}"
            fetch-url="{{ route('specifications.fetch-item', $item->id) }}"
        ></specifications-view>
    </section>
</div>

@endsection